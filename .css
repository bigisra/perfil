.img-responsiva {
    max-width: 100%;
    height: auto;
    display: block;
}

img {
    max-width: 100%;    /* Não deixa a imagem vazar da tela */
    height: auto;       /* Mantém a proporção */
    display: block;     /* Remove espaços vazios abaixo da imagem */
    border: 0;          /* Remove bordas automáticas de links em navegadores antigos */
    object-fit: contain; /* Garante que a imagem caiba no espaço sem ser cortada */
}